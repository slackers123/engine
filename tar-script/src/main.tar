#def entry = main;
#use std::log;

fn main() {
    int i = 0;
    while (i < 10) {
        fib(1.0f, 1.0f, 0);
        i = i + 1;
    }
}

fn fib(a: int, b: int, d: int) {
    log(a);
    if (d < 100) {
        fib(b, a+b, d+1);
    }
}
